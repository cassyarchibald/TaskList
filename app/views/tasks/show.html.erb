<% if @task.nil? %>
  <h1> 404 Not found</h1>
<% else %>
  <h1><%= @task[:name] %></h1>
  <p><%= @task[:description] %></p>
  <p><%= @task[:completion_date] %> </p>
<% end %>

<%= link_to "Edit Task", edit_task_path(@task.id) %>
<%= link_to "Delete Task", destroy_task_path(@task.id) %>
